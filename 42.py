lista = []
for v in range(1,11):
    lista1 = v**2
    lista.append(lista1)
print(lista)
# Criamos uma lista vazia, depois fizemos um range de 1 ate 10
# então criamos outra lista que receberia o range elevado a 2
# adicionamos a lista criada no loop a lista criada no começo
# imprimimos a lista